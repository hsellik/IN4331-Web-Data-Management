DB_IDENTIFIER=WebDataManagementsOrders
PGPORT=5432
PGDATABASE=DBOrders
PGUSER=master
PGPASSWORD=master-pwd
CREATE_TABLE="CREATE TABLE IF NOT EXISTS orders(
      Item_ID VARCHAR (50) PRIMARY KEY,
      quantity INTEGER DEFAULT 100
    );"