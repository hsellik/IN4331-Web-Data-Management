DB_IDENTIFIER=WebDataManagementPayment
PGPORT=5432
PGDATABASE=DBPayment
PGUSER="master"
PGPASSWORD="master-pwd"
TABLE_NAME=Payment
DBINSTANCE_CLASS="db.t2.micro"

CREATE_TABLE="CREATE TABLE IF NOT EXISTS $TABLE_NAME(
      Orde_ID VARCHAR (50) PRIMARY KEY,
      isPaid INTEGER DEFAULT 100
    );"