DB_IDENTIFIER=WebDataManagementStock
PGPORT=5432
PGDATABASE=DBStock
PGUSER="master"
PGPASSWORD="master-pwd"
TABLE_NAME=Stock
DBINSTANCE_CLASS="db.t2.micro"

CREATE_TABLE="CREATE TABLE IF NOT EXISTS $TABLE_NAME(
      item_id VARCHAR (50) PRIMARY KEY,
      quantity INTEGER DEFAULT 100
    );"
